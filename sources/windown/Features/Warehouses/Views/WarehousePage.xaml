<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="windown.Features.Warehouses.Views.WarehousePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowDefinitions="Auto,*">
        <StackPanel Grid.Row="0" Spacing="4" Margin="0,0,0,20">
            <TextBlock Text="Quản Lý Kho &amp; Tồn Kho Vàng Bạc" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="SemiBold" />
            <TextBlock Text="Theo dõi tồn kho vàng trang sức, vàng miếng và lịch sử điều chuyển giữa các chi nhánh" 
                       Style="{ThemeResource BodyTextBlockStyle}" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <Border Grid.Row="1" 
                Background="{ThemeResource LayerFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="20">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                <FontIcon Glyph="&#xE8F1;" FontSize="48" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}" />
                <TextBlock Text="Quản lý tồn kho theo từng món, tuổi vàng và tổng khối lượng" 
                           Style="{ThemeResource SubtitleTextBlockStyle}" 
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
